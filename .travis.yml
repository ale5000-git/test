sudo: false
language: python
matrix:
  include:
    - os: linux
      python: 2.6
    - os: linux
      python: 3.4
    - os: linux
      python: pypy
    - os: osx
      osx_image: xcode7.2
      language: generic
  allow_failures:
    - os: osx
env:
  - RUN_TYPE=dumb
addons:
  apt:
    packages:
      #- android-tools-adb
      #- p7zip-full
      #- zipalign

install: .travis/install.sh

script:
  #- nosetests
  - python test.py
